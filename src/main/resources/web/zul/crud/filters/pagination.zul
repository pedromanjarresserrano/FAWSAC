<paging viewModel="@id('cp') @init('com.gitlab.pedrioko.core.zk.viewmodel.crud.filters.Pagination')"
        pageSize="@load(cp.pagesize)"
        totalSize="@bind(cp.count)"
        activePage="@bind(cp.activepage)"
        onPaging="@command('paging')">

</paging>